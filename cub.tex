  \input{styles/kapfhammer-solarized-tikz-colors.tex}
  \hspace{-.25in}

  \begin{center}

    \begin{tikzpicture}[thick,scale=1, every node/.style={scale=1}]

      \path[use as bounding box] (-3.0,2.5) rectangle (10,-2);

      \path[->]<1,3-> node[proc, minimum width=7ex]
      (input1) at (0,1.5) {Input};

      \path[->]<2> node[proctest, minimum width=7ex]
      (input1) at (0,1.5) {Input};

      \path[->]<2-> node[io, below of=input1, yshift=-0.3in,
      xshift=0.0in,minimum width=11ex] 
      (runtime1) {Runtime = 1};


      \path[->]<1-2,4-> node[proc, right of=input1, yshift=0.0in,
      xshift=1.25in,minimum width=7ex,minimum height=4*\heightof{Input}] 
      (input2) {Input};

      
      \path[->]<3> node[proctest, right of=input1, yshift=0.0in,
      xshift=1.25in,minimum width=7ex,minimum height=4*\heightof{Input}] 
      (input2) {Input};


      \path[->]<3-> node[io, below of=input2, yshift=-0.3in,
      xshift=0.0in,minimum width=11ex] 
      (runtime2) {Runtime = 8};

      \path[->]<4-> node[io, below of=input1, yshift=-0.6in,
      xshift=0.75in,minimum width=11ex] 
      (ratio) {Ratio = 8};

      \path[->]<5-> node[special, below of=ratio, yshift=-0.3in,
      xshift=0.0in,minimum width=11ex] 
      (takeaway) {CUBIC};

    \end{tikzpicture}

  \end{center}
